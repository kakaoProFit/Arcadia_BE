spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3307/bootex
    username: root
    password: 5451
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
      use-new-id-generate-mappings: false
    show-sql: true
    properties:
      hibernate.format_sql: true
  mybatis:
    type-aliases-package: "package profit.arcadia.vo"
  mvc:
    view:
      prefix: "/WEB-INF/view"
      suffix: ".jsp"
server:
  port: 8080
