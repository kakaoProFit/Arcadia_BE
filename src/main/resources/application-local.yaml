## 로컬 환경에서 실행할 수 있도록 설정
## 개인마다 로컬 설정은 다를 수 있기때문에 보편적인 설정으로 통일, 필요시 각자 수정
## mysql 필요
## mongodb 서버 실행
## redis 서버 실행
## oauth 미작동
## smtp 임시 유저 데이터 작성중

spring:
  datasource:
#    url: jdbc:mariadb://localhost:3306/
#    SQL 쿼리문으로 ` create database arcadia_local; ` 실행
    url: jdbc:mysql://localhost:3306/arcadia_local
    username: root
    password: 1234
#    driver-class-name: org.mariadb.jdbc.Driver
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    open-in-view: true

  data:
    redis:
      host: localhost
      port: 6379
      password: 1234
    mongodb:
      database: test
      uri: mongodb://localhost:27017/

  mail:
    host: smtp.gmail.com
    port: 587
    username: m2nuuu@gmail.com
    password: rdqx azkf kkjk nosc
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: dummy
        secret-key: dummy
      s3:
        bucket: local-bucket

  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: dummy
            client-secret: dummy
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/{action}/oauth2/code/{registrationId}"
            scope:
              - name
              - email
              - profile_image
            client-name: Naver
          kakao:
            client-id: dummy
            client-secret: dummy
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/{action}/oauth2/code/{registrationId}"
            scope:
              - profile_nickname
              - profile_image
              - account_email
            client-name: Kakao
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-info-authentication-method: header
            user-name-attribute: response
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-info-authentication-method: header
            user-name-attribute: id
